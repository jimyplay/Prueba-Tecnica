# Supabase (project ref xdjhaquxrryarhksocbs)
NEXT_PUBLIC_SUPABASE_URL=https://xdjhaquxrryarhksocbs.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Resend (NOT used by the app at runtime — the reminder cron reads its key from
# Supabase Vault. This is only needed by the "enviar licitacion" route, which
# sends the initial email with attachment synchronously from Next.js.)
RESEND_API_KEY=
